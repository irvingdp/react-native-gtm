const ReactNativeGtmIOS = {
    openContainerWithId : function(Module, containerId, debug = false, defaultContainerName = '') {
        return Module.openContainerWithId(containerId, debug);
    },
}

export default ReactNativeGtmIOS;
